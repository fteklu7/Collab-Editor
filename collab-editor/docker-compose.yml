version: "3.9"

services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      PORT: 4000

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
